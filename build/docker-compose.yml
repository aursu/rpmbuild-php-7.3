version: "3.5"
services:
  centos7php73build:
    networks:
      - buildnet
    build:
      context: .
      network: buildnet
      args:
        centos: $OS7
    image: "aursu/php73build:${OS7TAG}-build"
  centos7php73buildrel:
    networks:
      - buildnet
    build:
      context: .
      dockerfile: Dockerfile.rel
      network: buildnet
      args:
        centos: $OS7
    image: "aursu/php73build:${OS7TAG}-build-rel"

networks:
  buildnet:
    external: true
    name: buildnet
