version: "3.5"
services:
  centos7php73dev:
    build:
      context: dev
      args:
        centos: $OS7
    image: "aursu/php73build:${OS7TAG}-dev"
  centos8php73dev:
    build:
      context: dev
      args:
        image: devbase
        centos: $OS8
    image: "aursu/php73build:${OS8TAG}-dev"